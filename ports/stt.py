"""Port: Streaming speech-to-text (OpenRouter Whisper-class / faster-whisper) (spec §20).

Interface stub — method signatures are defined when the module is built,
after reading its spec section.
"""

from typing import Protocol


class STT(Protocol):
    """Streaming speech-to-text (OpenRouter Whisper-class / faster-whisper)."""
