"""Port: Speech emotion recognition (emotion2vec GPU service client) (spec §22).

Interface stub — method signatures are defined when the module is built,
after reading its spec section.
"""

from typing import Protocol


class SER(Protocol):
    """Speech emotion recognition (emotion2vec GPU service client)."""
