"""Port: Speech synthesis (Grok Voice via OpenRouter, inline prosody tags) (spec §23).

Interface stub — method signatures are defined when the module is built,
after reading its spec section.
"""

from typing import Protocol


class TTS(Protocol):
    """Speech synthesis (Grok Voice via OpenRouter, inline prosody tags)."""
