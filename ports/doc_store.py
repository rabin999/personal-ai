"""Port: Document store (MongoDB) — config, profiles, projects, ledger, cost (spec §1).

Interface stub — method signatures are defined when the module is built,
after reading its spec section (CLAUDE.md §1).
"""

from typing import Protocol


class DocStore(Protocol):
    """Document store (MongoDB) — config, profiles, projects, ledger, cost."""
